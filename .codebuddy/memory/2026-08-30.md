# 2026-08-30

- 修复 AI 翻译线程 json.exception.type_error.316 崩溃：根因是 `clean_utf8` 只检查续字节格式，放过过编码（0xC0 0x80）、代理区（0xED 0xA0+）、超范围（0xF4 0x90+）等"伪合法"UTF-8 序列；nlohmann parse 阶段原样存储，dump 时严格校验抛出 316。已严格化 `clean_utf8` 校验，并新增 `safeDump()` 用 `json::error_handler_t::replace` 兜底所有 `dump()` 调用（发请求、写 _已翻译.json、写 翻译失败.json）。
- 修复翻译规则：sysMsg 第 7 条与 `_未翻译.json` 写入的"翻译规则"第 4 条，由"仅当中英文意思完全相同时才可去括号"改为"强制保留括号"，避免 None → 无 这类不带括号的输出。
- AI 请求 `max_tokens` 由 8192 提高到 16384，缓解大肥鱼 deepseek-v4-flash 思维链占满 token 导致 JSON 输出被截断/为空的问题。
- 版本号升至 v2.2.5，更新 README.md 更新日志，编译通过（0 错误 0 警告），Release exe 已部署到 `E:\Program Files\Game-Related\FFXIV_MOD_Options_Chinese_AI-Translated_v2.2.5\`。
- 用户把"每批条数"改为 100；已说明 100 条会增大思维链压力，建议先用 100 跑一轮，如仍失败再临时改回 50/30。
- 修复「英文（英文）」重复格式（EXQB（EXQB））：根因是上一版第 7 条"强制保留括号"导致 AI 无法翻译的专名机械套括号。三层兜底：(1) sysMsg 第 7 条与"翻译规则"第 4 条改为"能译中文才用括号，无法翻译的专名原样保留英文，禁止英文（英文）"；(2) 新增 `fixRepeatParen()`，AI 译文写入前把 `X（X）`（无中文）回退为 `X`；(3) `dictFillDisplay()` 词典译名无中文时直接返回不再拼括号。并入 v2.2.5 发布（版本号不递增），已编译部署 E 盘 v2.2.5 目录。注意：历史 `_已翻译.json` 中的旧数据不会自动修正，需重跑或手动改。
- 用户询问"给外部 AI 看的翻译规则"是否同步：确认 `_未翻译.json` 的 `翻译规则` 字段（cpp 内 `out["翻译规则"]`，约 648-659 行）已同步，config.default.json 无其他规则出口。顺手把外部规则第 2 条"格式要求"补上专名例外（"确实无法翻译的专名/缩写/品牌名原样保留英文，不得输出英文（英文），见第 4 条"），与第 4 条自洽。已编译部署。
- 用户要求内置预设「智谱 GLM」默认模型由 `glm-4.7-flash` 改为 `GLM-4.5-Air`：改了 cpp `g_defaultAIPresets`（约 258 行）与 config.default.json（原已是 glm-4.5-air 小写，统一为 GLM-4.5-Air），README 更新日志补充，并入 v2.2.5，已编译部署 E 盘。注意：运行中的程序需重新点「选择 AI 配置」套用「智谱 GLM」才会用新模型；若 config.user.json 里存在同名自定义预设会覆盖内置值。
- 修复"自定义 AI 模型名保存了却用不上"：根因在 `BuildAISelectList`——自定义记录与内置预设同名/同地址时被合并成一行，但合并行的 model/baseUrl 用内置预设值，用户自定义的 model 被丢弃。已改为合并时自定义记录的 model/baseUrl 优先；合并条件去掉 `!s.key.empty()`（无 Key 的自定义记录也合并，避免出现两条智谱）；`NextPresetName()` 同时检查 customSaves 防止"自定义 N"重名覆盖。并入 v2.2.5，已编译部署。用户需删掉旧的错误记录重新保存，或直接编辑 config.user.json。
- 重要事实：用户实际运行的是 `E:\Program Files\Game-Related\FFXIV_MOD_Options_Chinese_AI-Translated_v2.2.3\` 目录（config.user.json 在 v2.2.3 目录），不是 v2.2.5 新目录。以后部署 exe 必须覆盖到 **v2.2.3 目录**用户才用得上（已把 v2.2.5 exe 复制覆盖到 v2.2.3 目录）。用户配置：词典目录 `E:\FFXIV_MOD\词典目录`（唯一词典 `wiki_术语对照and个人填充.json`，结构 `{_descriptions:{}, _options:{}, terms:{英文:中文}}`），翻译目录 `E:\FFXIV_MOD\翻译目录`，大肥鱼 Key 名称「蓝色大肥鱼」，智谱 Key 名称「智谱有key」。
- 一次性数据清洗（PowerShell 脚本，已完成）：`E:\FFXIV_MOD\翻译目录\_已翻译.json` 里所有 `X（X）`（X 无中文，如 75%（75%））回退为 X，共修 7 条；并把「皇帝上衣染色（Emperor's Top Dye）」等修正为「皇帝的新衣·上衣染色（Emperor's Top Dye）」（用户要求按童话"皇帝的新衣"系列命名，wiki 官方词条是 The Emperor's New Attire→皇帝的新装备套装）。词典 `wiki_术语对照and个人填充.json` 的 terms 已补 `Emperor's Top Dye`→皇帝的新衣·上衣染色、`Emperor's Bottom Dye`→皇帝的新衣·下装染色。注意：清洗用 ConvertTo-Json 写回，`&`、`'` 会被转义为 \u0026、\u0027（JSON 合法，程序读取无影响）；备份文件 .bak 已留在原目录。PS 的 ConvertFrom-Json 无法解析词典（含大小写重复 key The Blackest Night/the Blackest Night），故词典用正则插入而非解析。
- 用户澄清运行方式：用 Visual Studio 重新生成后 F5 运行，exe 与配置都在项目 `x64\Release\`（及 Debug）目录，不用 E 盘部署。已做：(1) `.vcxproj` 四个配置都加 PostBuildEvent 自动 xcopy 根目录 `config.default.json` 到输出目录（已编译验证生效）；(2) 手动同步 x64\Release、x64\Debug 的 config.default.json 为最新（智谱 GLM=GLM-4.5-Air）；(3) 更新 x64\Release\config.user.json 的 aiPresets 里「智谱 GLM」model 为 GLM-4.5-Air（用户配置：aiBatchSize=100，customSaves 蓝色大肥鱼/智谱GLM-4.5-Air，词典 E:\FFXIV_MOD\词典目录，翻译 E:\FFXIV_MOD\翻译目录，penumbra E:\FFXIV_MOD\penumbra）。已编译 0 错误 0 警告。以后不再部署 E 盘 exe。
- 查明 `Uranus`/`EXQB` 语义：`E:\FFXIV_MOD\penumbra\[HS] Cultist - Bodycon Beauty (Default)` 是 FFXIV 装备 mod（Cultist 作者，heliosphere.app 发布，目录名 [HS] 前缀不代表 Honey Select），选项里的 `Uranus`/`EXQB`/`Rue`/`Lava-*`/`Masc-*` 都是**体型/骨架 mod 专名**（Uranus Redux 等），不是天文词，按规则应保留英文不翻译。
- 默认黑名单加入 `Uranus`、`EXQB`（用户要求，可自行修改）：(1) 头文件 `AppConfig.blacklist` 默认值改为 `{"Uranus","EXQB"}`（内置兜底）；(2) cpp `LoadConfigFrom` 黑名单覆盖逻辑改为——默认层整体覆盖，**用户层仅非空数组才覆盖**（空数组=用户没设置过，保留默认专名），防止旧 user config 的 `blacklist:[]` 冲掉默认；(3) 三处 config.default.json（根、x64\Release、x64\Debug）blacklist 同步为 `["Uranus","EXQB"]`。黑名单机制：提取时（815-823 行子串匹配 toRemove）与应用时（is_blacklisted 跳过+还原）生效，词典目录 `单词黑名单.json`（用户已有 rue/bibo）+ config blacklist 合并。**注意：Rue/Masc/Lava 等短词有子串误伤风险（rue→true、masc→masculine）不能直接入默认黑名单。** 已编译 0 错误 0 警告。
- **重大 bug 修复：`fixRepeatParen` 从未生效过**（用户发现新翻译仍出现 75%（75%））。根因：全角括号 `）` UTF-8 占 3 字节，代码 `close != t.size()-1` 用右括号起始字节（9）比 size-1（11）永远不相等→函数直接 return；且 `substr(open+1, ...)` 指向括号中间字节导致 b 计算错误。已改为 `close + 3 != t.size()` 判断末尾 + `substr(open+3, ...)` 取内容。此前 EXQB（EXQB）问题靠提示词+清洗兜底掩盖了此 bug。
- `poly` 语义：3D 建模术语，poly=polygon 多边形缩写，higher poly=高多边形/高模。提示词已加：sysMsg 新增第 12 条（3D 建模术语），外部翻译规则新增第 9 条（同步两处）。词典 `_options` 补了完整 key `[HS] Cultist - Bodycon Beauty (Default)/group_009_cardigan options.json||Opt||Fuzzy layer (higher poly)` → `毛绒层（高多边形）`。
- 已清洗 `E:\FFXIV_MOD\翻译目录\_已翻译.json`（今天 09:48 新生成的）：通用 X（X）无中文回退 2 处（75%、90%），毛绒层坏条目 1 处（亚伊太利斯的明天 poly 幻觉→毛绒层（高多边形））。备份 .bak 已留。**教训：脚本往词典 `_options` 空对象插入条目时不能带尾逗号（`{ "k":"v",}` 非法）**，已修复为换行后 `},`。
- **自动清理 .json.bak 功能**（用户要求，2026-08-30）：penumbra 下 61 个 `.json.bak` + 词典目录 2 个（`.json.bak`/`.json.bak2`）是手动/脚本备份残留（非程序生成；程序备份是 `时间戳备份.zip` 和 `.损坏备份`）。已新增 `CleanupJsonBakFiles()`（cpp，LoadConfig 函数前）：程序启动时递归清理 `g_cfg.penumbraDir` + 非递归清理翻译目录、词典目录下以 `.json.bak`/`.json.bak2` 结尾的文件；在 WM_INITDIALOG 的 `LoadConfig()`+`LoadCustomSaves()` 后调用。现有 63 个已手动删除。已编译 0 错误 0 警告。
- **Yanilla 翻译**：`group_002_skirt - tycoon tights.json` 里 `Yanilla` 是体型骨架专名（YAB 作者 Yan 的 vanilla 风格体型，与 Rue/Belly/Uranus/EXQB 并列），原样保留英文正确。已加入默认黑名单：`config.default.json` 第 25 行 + `FFXIV_MOD选项汉化工具.h` 内置值 → `["Uranus","EXQB","Yanilla"]`。
- **hyurs 翻译修正**：用户发现 `hyurs` 被 AI 翻成"海德林人（hyurs）"多了"人"字。原因：词典只有 `Hyur→人族`（大写单数），`hyurs`（复数小写）未命中词典走 AI，AI 用了民间称呼"海德林人"。已修：词典 `wiki_术语对照and个人填充.json` terms 加 `"hyurs": "人族"`，`_已翻译.json` 第 338 行改为 `"人族（hyurs）"`。同组种族选项（elezen 精灵族、miqote 猫魅族等）均走官方译名+族。
- **编译教训**：Rebuild 时偶发 LNK1104（exe 被占用，用户开着 devenv.exe），直接重试一次即可，无需 kill 进程。
- **已翻译文件全面查漏补缺**（2026-08-30）：通读 `_已翻译.json`（362 选项+10 描述），脚本对比 `_未翻译` 无缺失无空值，问题全在翻译质量。修正 9 处：①Lava→Lava（Lavabod 简称，AI 误翻"岩浆"）②Masc→Masc（YAMasc 简称，误翻"男性化"）③Tycoon Bootlets→**误改又改回**：先改成"泰空短靴"，用户指出应以 wiki 官方为准，查 wiki 词典官方译名就是"泰空公主高跟鞋"，已恢复（重要教训：**改翻译前先查 wiki 词典**）④Yet Another Manicure→又一款美甲⑤Lavabod Teardrop→Lavabod 泪滴（与 Buff 版统一）⑥Lavabod→Lavabod（专名，误翻"岩体"）⑦Highlander/Roe→高地之民/鲁加族（Roe=Roegadyn 简称漏翻）⑧Ballerina→芭蕾舞者（两处统一）⑨Reaper→钐镰客、Viper→蝰蛇剑士（官方译名）。词典补充 5 词条：Roe→鲁加族、Lava→Lava、Lavabod→Lavabod、Lavabod Teardrop→Lavabod 泪滴、Masc→Masc。已写 Python 脚本 `D:\Fast folder\Document\_scripts\dict_check.py` 比对 _已翻译.json 与 wiki 词典 terms：命中 40 条全部一致。**注意**：同一文件多个 replace_in_file 并行调用会互相覆盖（后写覆盖先写），同一文件的多次替换必须串行；词典里本就存在重复 key（The Blackest Night/the Blackest Night），nlohmann json 可读（后值覆盖），PowerShell ConvertFrom-Json 会报错，需用 Python json 处理；FFXIV wiki 词典有 Tycoon 系列官方译名（泰空公主XX）。
- **内置 wiki 原典机制**（用户要求，2026-08-30）：用户导出的 `wiki_术语对照.json`（3.3MB/6.2万条）复制为项目根 `内置wiki_术语对照.json` 随 exe 打包发布。(1) vcxproj Release|x64 PostBuildEvent 加一行 xcopy 内置文件到输出目录；(2) build_release.ps1 Step 2c 复制到 release（**坑**：ps1 被 PowerShell 按 ANSI 解码 UTF-8 中文，中文字面量会乱码，需用 `[char[]](0x5185,...)` Unicode 码点构造文件名，同文件里 日志.json 原脚本就是这么做）；(3) cpp 新增 `ReleaseBuiltinWiki(dictDir)`（1596 行）：词典目录无 `wiki_术语对照.json` 且 exe 旁有内置版时复制释放，已有文件不覆盖；在 `EnsureDictFile`（1630 段）与 `WikiImportThread` 原典缺失分支（2279 段，优先于从汇总词典复制）调用。wiki 导出流程本就"先写回原典 wiki_术语对照.json 再 SyncWikiToMerged 同步新增进汇总词典"，用户"重复跳过、新增才添加"需求天然满足。已编译 0 错误 0 警告，x64\Release 与 release\ 均含内置文件。
- **打包命令**：`build_release.ps1 -Zip`（不带 -Zip 只出 release\ 目录）。zip 输出 `D:\Fast folder\Downloads\压缩文件\FFXIV_MOD_Options_Chinese_AI-Translated_v<版本号>.zip`（版本从 README 变更日志解析）。打包=编译 Rebuild + 复制 release + 打 zip，不推送 E 盘（约定：只用项目 x64\Release 运行，不部署 E 盘）。
- **AI 预设扩充**（2026-08-30）：`g_defaultAIPresets`（cpp 256 行）与三处 config.default.json（根、x64\Release、x64\Debug，Debug 顺带补回 Yanilla 黑名单）从 2 个预设扩到 11 个：新增 通义千问(qwen-plus/百炼 compatible-mode/v1)、Kimi(kimi-latest/api.moonshot.cn/v1)、豆包(火山方舟 ark.cn-beijing.volces.com/api/v3)、腾讯混元(hunyuan-turbo)、百度千帆(ernie-4.0-turbo-8k/qianfan.baidubce.com/v2)、硅基流动(siliconflow.cn/v1)、OpenRouter、Groq、OpenAI(GPT,注国内不可直连)。**主动测试**：用 curl 发假 key POST 测连通性——401/403=地址可达仅缺 key；通义 400（走通业务）；OpenAI 官方 000 不可达（国内需代理）；**Anthropic 官方端点 /v1/messages 与程序拼 /chat/completions 不兼容，未加入**。程序 URL 拼接逻辑（2822-2824 行）：baseUrl 去尾部斜杠，若不含 /chat/completions 就拼上，故预设 baseUrl 须写到路径前缀为止。customSaves 保存无数量上限（SaveCustomSaves 同名覆盖否则 push_back，NextPresetName 无限递增）。用户升级时 config.user.json 预设按名称与默认层合并，新预设自动出现。
- **海外预设补全**（2026-08-30，接上条）：用户本机代理 127.0.0.1:7890（系统代理 ProxyEnable=1，HKCU Internet Settings，WinHTTP 仍直连不受影响）。curl -x 走代理实测 7 个海外端点全部可达（401=缺 key 到达、400=到达仅请求体细节）：OpenAI、xAI Grok(api.x.ai/v1)、Mistral(api.mistral.ai/v1)、Google Gemini(generativelanguage.googleapis.com/v1beta/openai)、Anthropic(api.anthropic.com/v1)、OpenRouter、Groq。因程序只发 OpenAI 兼容 /chat/completions，Anthropic 必须用官方 OpenAI 兼容层端点 **api.anthropic.com/v1**（其 /v1/messages 不兼容）。已把 4 个新预设加进 cpp `g_defaultAIPresets`（268-271 行）与根 config.default.json（15 个）：Google Gemini(gemini-2.0-flash)、Anthropic Claude(claude-sonnet-4-20250514)、xAI Grok(grok-2-latest)、Mistral(mistral-small-latest)，note 均注明"国内不可直连需代理"。编译 Rebuild 0 错误 0 警告，build_release.ps1 -Zip 打包到 `D:\Fast folder\Downloads\压缩文件\FFXIV_MOD_Options_Chinese_AI-Translated_v2.2.5.zip`。**坑**：PostBuildEvent 只在 Release|x64 生效，x64\Debug 的 config.default.json 需手动复制同步（已同步 15 个）；ps1 脚本里中文路径/字面量会按 ANSI 读乱码，一律用 `[char[]](码点)` 构造（`选项汉化工具`=0x9009,0x9879,0x6C49,0x5316,0x5DE5,0x5177）。
- **_已翻译.json 重新生成后全面修正**（2026-08-30，用户删了旧翻译重跑后新文件 427 条=8描述+419选项，之前手工修过的又回来了）：用 Python 脚本按完整 key 精确替换 58 处。要点：①体型专名 Rue/Masc/Lava/Lavabod 全保留英文（如 Flat Rue、Masc Legs、Lavabod Rue、Rue Mini/SC/WC、Rue Feet 及组合 Buff 版，去括号）②"No highlights/No Collisions" 被 AI 误译成"摇头 highlights"→改"无高光/无碰撞"③Emperor's Top/Bottom Dye→皇帝的新衣·上衣/下装染色 ④True/Dark/Light patterns 组合统一补全"真实图案"⑤Ballerina 统一芭蕾舞者（AI 乱译"芭蕾舞鞋/芭蕾"）⑥Highlander/Roe→高地之民/鲁加族、Midlander/Miqo/Elezen→中原之民/猫魅族/精灵族 ⑦Stretch Marks→妊娠纹（AI 译"伸展 Marks"）⑧Reaper/Viper→钐镰客/蝰蛇剑士（官方译名）⑨hyurs→人族（又见"海鲁人"）⑩补括号格式 2 处。备份留在 `E:\FFXIV_MOD\翻译目录\_已翻译.fixbackup.json`（.fixbackup 后缀不会被程序启动清理，需用户自行决定去留）。**教训**：重新生成的 _已翻译.json 会丢掉所有词典补录之外的手工修正，专名类（Rue 等）应进词典或黑名单才能持续命中；Python json 处理 JSON 安全（不转义 & '，保留顺序），命令行传中文路径会乱码须用脚本内 r'' 字面量。
- **14 项需求批量完成**（2026-08-30，用户 14 连发，版本提升至 v2.2.6 记录在 README）：①提取英文自动创建单词黑名单.json（ExtractEnglish 开头，文件不存在时 SaveBlacklistFile 合并列表）②Buff 用"健壮"（词典 terms 补 Buff→健壮）③默认黑名单加 Rue、Lavabod（.h 内置值 + 根/release/x64 Release/x64 Debug 的 config.default.json + **x64\Release\config.user.json 用户层**都加了，用户层非空覆盖默认所以必须同步）④sysMsg 第 13 条 + 外部翻译规则第 10 条新增"无歧义固定词"（Feet=脚部等身体部位）⑤Masc 判断：masculine 缩写但 YAMasc 体型专名有歧义，保留英文不入黑名单 ⑥词典 terms 补 Skull Crushers→碎颅大腿肌肉、Watermelon Crushers→碎瓜大腿肌肉，Rue:芸香→Rue；_已翻译.json 4 处"碎颅/碎瓜"→"碎颅大腿肌肉/碎瓜大腿肌肉"；SC=Skull Crushers、WC=Watermelon Crushers 确认，但 Rue 在黑名单故 Rue SC/WC 整体保留英文，短词 SC/WC 不补词典防误伤 ⑦AI 设置名称框加"名称:"标签（rc 布局：名称 x=55、API Key 标签移到 185、key 框 250 宽 187）⑧"越小越稳"改"每批发给AI翻译的英文条数"⑨删"打开用户配置"按钮（rc+cpp case+OpenUserConfigFile 函数+Resource.h 宏）⑩"测试"→"测试链接" ⑪导入对话框改 LBS_MULTIPLESEL 点中即勾选、g_importFile→g_importFiles 向量、RunImportThread 循环导入、子类化拦截 Ctrl+A 全选 ⑫⑬按钮改名"2. 已翻译写入词典"/"3. 词典写入Mod"（第一排 4 按钮改 130 宽 x=10/150/290/430），检查翻译按钮移到 300 ⑭README 使用说明+更新日志 v2.2.6+主界面流程/辅助行+日志指引同步。编译 0 错误 0 警告。**注意**：改 config.user.json 后若程序在运行退出会写回覆盖，用户需重启程序；Rue 子串误伤（true/screw 等）用户已知情接受。
- **碎颅/碎瓜改"肌肉大腿"**（2026-08-30，用户说"大腿肌肉"不合适）：词典 terms Skull Crushers→碎颅肌肉大腿、Watermelon Crushers→碎瓜肌肉大腿；_已翻译.json 4 处同步（"碎颅大腿肌肉（Skull Crushers）"→"碎颅肌肉大腿（Skull Crushers）"等）。脚本已删，JSON 校验完整。
- **zip 包内含版本号顶层文件夹**（2026-08-30，用户要求 7z 解压到当前位置即得带版本号文件夹）：build_release.ps1 Step 3 改为——建 %TEMP%\ffxiv_mod_pkg_<guid> 暂存目录，把 release 内容复制进 `FFXIV_MOD_Options_Chinese_AI-Translated_v<版本>` 子目录，再 Compress-Archive 该顶层目录；同时 zip 目标已存在时改为追加 `_0/_1` 后缀（原 Remove-Item 覆盖逻辑删除，符合用户"防覆盖"偏好）。已打包验证：`D:\Fast folder\Downloads\压缩文件\FFXIV_MOD_Options_Chinese_AI-Translated_v2.2.6.zip`（1.28MB），zip 内条目为 `FFXIV_MOD_Options_Chinese_AI-Translated_v2.2.6/{config.default.json, FFXIV_MOD选项汉化工具.exe, 使用说明.txt, 内置wiki_术语对照.json, 日志.json}`。
- **git 中文 commit 的稳妥做法**（2026-08-30 教训）：PowerShell 命令行传中文参数（git commit -m 中文）乱码；execute_command 的 shell 还会把 `$` 变量插值展开（诊断命令里的 $c 被清空）。**最稳方案：write_to_file 写 UTF-8 提交信息到 `D:\Fast folder\Document\_scripts\gitmsg.txt`，再 `git commit -F <路径>`**（git 按 UTF-8 读文件，完全避开命令行中文）。本轮已创建 `D:\Fast folder\Document\_scripts\cz.ps1`（UTF-8 执行器，记忆里曾记录但实际不存在），但实测在本环境执行无输出/不生效，暂以 -F 方案为准。v2.2.6 本地提交 commit 2148b41（10 文件，含新入库的内置wiki_术语对照.json），未推送（等用户明确说"推送"）。
- **zip 包顶层文件夹定为中文固定名**（2026-08-30 最终定版）：经用户确认，`build_release.ps1` Step 3 的 `$innerName` 定为中文 `最终幻想14_mod选项汉化工具_AI翻译版`（无版本号），zip 文件名仍保留版本号。**码点序列**：`0x6700,0x7EC8,0x5E7B,0x60F3,0x31,0x34,0x5F,0x6D,0x6F,0x64,0x9009,0x9879,0x6C49,0x5316,0x5DE5,0x5177,0x5F,0x41,0x49,0x7FFB,0x8BD1,0x7248`（注意：mod 后无下划线，直接接"选项汉化工具"）。已 Rebuild 并打包验证：`D:\Fast folder\Downloads\压缩文件\FFXIV_MOD_Options_Chinese_AI-Translated_v2.2.6_1.zip` 内部条目均为 `最终幻想14_mod选项汉化工具_AI翻译版/{config.default.json, FFXIV_MOD选项汉化工具.exe, 使用说明.txt, 内置wiki_术语对照.json, 日志.json}`。旧包 `..._0.zip`（英文顶层名）和 `..._2.2.6.zip`（带版本号）仍在 Downloads，用户可自行删除。
- **5 项 UI 修改批量完成**（2026-08-30，并入 v2.2.6 不升版本号，编译 0 错误 0 警告）：①「API Key」→「API_Key」：rc 标签（102 行）、配置列表列标题（cpp 3761 行）、日志/提示文案（未设置/尚未配置 API_Key）全部统一。②配置列表 Key 列：预设无 Key 显示空值（不再显示「沿用当前」），有 Key 默认掩码 `******`；套用配置时去掉 customSaves 自动匹配 Key 逻辑（cpp 4669 行附近），预设自带 Key 才覆盖，否则保持当前 Key。③配置列表新增「增加」按钮（保存主窗口当前 AI 输入为自定义配置并刷新列表）+ 双击列表行直接确认套用（NM_DBLCLK→EndDialog IDOK）。④操作按钮「2. 已翻译写入词典」→「已翻译的文件，写入词典」（rc 按钮+流程说明+README 使用说明同步；README 更新日志历史不改）。⑤全部 Edit 填写框 Ctrl+A 全选：新增 `EditSelectAllProc`（SetWindowSubclass 子类化，WM_CHAR==1→EM_SETSEL 全选），主窗口 WM_INITDIALOG 与黑名单对话框调用 EnableEditSelectAll。
- **重大坑：IDE 资源编辑器会回滚 Resource.h 中手加的宏**（2026-08-30）：两次在 Resource.h 添加 `#define IDC_AI_SELECT_ADD 1255` 都被自动覆盖删除（.rc 的改动不回滚，仅 Resource.h）。解决：**rc 里控件 ID 直接用数字 1255，cpp 顶部用 `#ifndef IDC_AI_SELECT_ADD / #define IDC_AI_SELECT_ADD 1255 / #endif` 兜底**。以后新增控件 ID 建议一律走此方案，避免与 IDE 资源编辑器冲突。
- **更正**（2026-08-30，接 5 项 UI 修改）：需求③理解修正——用户要的是「配置列表**增加双击确认功能**」而非「增加按钮」。已撤销上一轮误加的「增加」按钮（rc 按钮行、cpp 的 IDC_AI_SELECT_ADD 宏与 WM_COMMAND 处理全部删除），仅保留 NM_DBLCLK 双击列表行即确认套用的功能。重新编译 0 错误 0 警告。
- **AI 翻译三层读取顺序补全**（2026-08-30）：用户询问 AI 翻译是否实现「黑名单→词典→原典」。诊断结论：黑名单（提取阶段子串剔除）、词典（AI 翻译前 LoadTermMap 预填唯一词典）原本就有；**原典兜底缺失**。已修：
  - `LoadTermMap`：去掉 `en == zh` 跳过（之前「保英文」词条如 Lava→Lava 写了也不生效的 bug）；并新增**原典兜底**——唯一词典未收录时补查 `wiki_术语对照.json` 的 terms（不覆盖唯一词典，唯一词典优先）。
  - AI 预填（AITranslateFile）与 `AutoFillJsonWithDict` 命中条件由 `tr != orig` 放宽为允许 `tr==orig`，保英文词条直接保留原文（dictFillDisplay 对纯英文译名本就原样返回）。
  - 已补回唯一词典个人词条：`Lava→Lava`、`Masc→Masc`、`Roe→Roe`（清空重跑重建词典会丢个人词条，注意备份）。
  - 编译 0 错误 0 警告。注意：MSVC 工具链在 `E:\Program Files\Microsoft Visual Studio\VC\`（vcvarsall.bat x64 + msbuild），不在 C 盘；命令行传中文路径会被 PowerShell5.1 按 ANSI 打乱，构建脚本需全 ASCII + 通配符定位项目目录。
- **汇总词典改名 + 个性翻译.json**（2026-08-30，版本 v2.2.7，编译 0 错误 0 警告）：①唯一词典文件名 `wiki_术语对照and个人填充.json` → `汇总词典.json`。新增 `MergedDictPath(dictDir)`：新名不存在且旧名存在时自动 rename 迁移（失败退回旧名），替换 cpp 全部引用点（ApplyTranslation/LoadTermMap/SyncWikiToMerged/ImportTranslations/Wiki 导出兜底/AI 预填/启动创建）。②新增 `个性翻译.json`：`EnsureCustomDictFile` 在 EnsureDictFile 中创建空对象；格式「英文:中文」或完整 key「路径||Name||原文:译文」；ApplyTranslation 在加载黑名单后、收集 rollbackMap 前做内存覆盖（dict["terms"] + _options/_descriptions 尾部原文匹配或完整 key 命中），只影响写入 Mod 的文本、不回写汇总词典文件。③黑名单 > 个性翻译：命中黑名单的英文词条跳过覆盖（应用时仍按黑名单还原英文）。④README 更新日志加 v2.2.7，rc 功能二文案同步。**注意**：cz.ps1 在本 execute_command 环境实测不生效（-File 与 -Command 调用均无输出/不写文件），构建改用 `powershell -NoProfile -Command "& 'D:\Fast folder\Document\C++\*\build_release.ps1'"`（通配符定位中文目录、全 ASCII）成功；`$` 变量在 execute_command 命令行会被剥（如 $_→.），避免在命令行内联使用。
