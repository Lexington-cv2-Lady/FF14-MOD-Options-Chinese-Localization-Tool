# 长期记忆

## FFXIV_MOD选项汉化工具

- 二级窗口交互规范（2026-08-31，v2.3.6 更新，覆盖旧 v2.3.5 规范）：「提取英文」「恢复备份」「翻译缺失」「Wiki 分类」等二级窗口统一——左侧 ListView（LVS_EX_CHECKBOXES | LVS_EX_FULLROWSELECT）：**单击行 = 勾选/取消勾选切换（toggle，可取消）**，双击行 = 确认勾选（NM_DBLCLK 设 TRUE，因单击已 toggle 一次）；checkbox 区（hti.flags & LVHT_ONITEMSTATEICON）由原生处理；LVN_ITEMCHANGED 只做右侧子列表刷新，不再联动勾选。右侧列表：带勾选框 ListView + NM_CLICK toggle + NM_DBLCLK 确认 + 右侧「全选」「取消全选」按钮（恢复备份 1207/1208、提取英文 1267/1268；翻译缺失 1277/1278 已有）。拉框/Shift/Ctrl 多选仍可用，主操作按钮对未勾选场景退回高亮选中项。「取消全选」= 取消选中 + 取消勾选。导入翻译窗口例外（用户明确要求无全选防误批量写入，保持 ListBox）。新增对话框控件 ID 需三处同步：rc 用字面量数字（防 IDE 资源编辑器回滚 Resource.h）、cpp 顶部 #ifndef 兜底宏、Resource.h 定义宏；ID 不得与已有冲突（如 1348 已属 IDC_BTN_CUSTOM）。注意 ListView_SetCheckState 宏展开后 if/else 必须带大括号（无括号报 C2181）；GetDlgItem 用 (int)nm->idFrom 防 C4244 警告（要求 0 警告）。「翻译缺失」窗口（IDD_MISSING_DIALOG=1270 系列）与 AI 翻译链路的对接：CollectMissingEntries() 扫描 MOD 收集缺失条目，key 组装与 ExtractEnglish 完全一致（rel||Name||原文 / rel||Desc||原文 / rel||Opt||原文），选中后生成 时间戳_未翻译.json 并把 g_aiTargetFile 指向它，RunAITranslateThread 开头检测 g_aiTargetFile 非空则直接翻译（failPath 须在 if/else 外声明）。

- 翻译规则同步约定（2026-08-30，用户明确要求）：凡修改 AI 翻译规则，必须同时同步两处——(1) 程序内置的 system prompt（`AITranslateBatch` 里的 `sysMsg`，约 2700 行附近）；(2) 写入 `_未翻译.json` 的「翻译规则」字段（`out["翻译规则"]`，约 648-659 行，这是给外部 AI 看的规则）。两处都要改，不能只改一处。
- 翻译规则相关：短名称用「中文（英文）」格式；无法翻译的专有名词/缩写/品牌名（如 EXQB、Uranus）原样保留英文，禁止输出「英文（英文）」重复格式；`fixRepeatParen()` 在 AI 译文写入前做兜底修正。
- 运行方式（2026-08-30 确认）：用户用 **Visual Studio 打开项目，Ctrl+Shift+B 重新生成后按 F5 运行**，实际运行的 exe 和配置在 `x64\Release\`（也可能 `x64\Debug\`）目录，**不是 E 盘部署目录**。因此：以后源码修改后无需往 E 盘部署 exe；改 `config.default.json` 后需同步到 `x64\Release\`（VS 项目已加 PostBuildEvent：`xcopy /Y /Q "$(ProjectDir)config.default.json" "$(OutDir)"`，用户每次重新生成会自动同步，无需手动）。`config.user.json`（用户 AI Key、词典/翻译/penumbra 目录、customSaves、aiPresets）在 x64\Release 目录，改它要小心程序退出时会用内存配置写回覆盖。
- 黑名单机制（2026-08-30 更新，v2.2.10）：来源 = 内置 `AppConfig.blacklist` 默认值（当前 `{"Uranus","EXQB","Yanilla","Rue","Lavabod"}`，即体型 mod 专名）+ config 各层 + 词典目录 `单词黑名单.json`（用户数据）。`LoadConfigFrom` 用户层黑名单**仅非空数组覆盖**（空=未设置，保留默认）。**v2.2.9 起提取英文不再跳过黑名单词**（ExtractEnglish 仅剩 `{"---","-"}` 分隔符过滤），黑名单词条全部进 _未翻译.json。**v2.2.10 起黑名单命中 = 整词完全匹配**（trim 后相等、忽略大小写）：单独的 Lavabod/Rue 整词仍保留英文；"Lavabod Teardrop" 这类组合不再整条被拦，正常翻译其余部分（专名由 AI 保留）；同时消除 rue→true 子串误伤；含黑名单词的组合词条现可在个性翻译.json 生效（customSkipped 同步跟随整词语义）。黑名单生效位置：AI 翻译时整词命中原样保留英文跳过翻译、应用（词典写入Mod）时跳过翻译并还原英文。**Masc 从不在黑名单**，保留英文靠词典 terms 保英文词条 `"Masc":"Masc"`（Masc=masculine 缩写、YAMasc 体型专名有歧义不入黑名单）。**AI 翻译预填链路优先级（v2.2.9）：黑名单 > 个性翻译.json > 汇总词典 > wiki 原典**；导入写入词典时个性翻译优先（个性翻译 > 汇总词典 先来后到）。FFXIV 常见体型 mod 专名：Uranus Redux（Uranus）、EXQB、Yanilla、Rue、Lavabod（Lava-*：Cupcake/Teardrop/Omoi/Frazetta/Sugar）、YAB、YAMasc（Masc-Flat/Pecs），全是专名应保留英文。
- 目录确定即建文件 + 自动迁移（2026-08-30，v2.2.8）：用户确定了词典目录/翻译目录路径（浏览选择或输入已存在目录）即立即建立 汇总词典.json、个性翻译.json、单词黑名单.json、wiki_术语对照.json（翻译目录则建目录），不再依赖点「1. 提取英文」等按钮；修改目录时自动把旧目录的词典文件（4 个）与翻译产物（*_未翻译、*_已翻译、翻译失败、*_翻译检查报告）迁移到新目录，新目录已有同名文件则不覆盖。实现：`EnsureBlacklistFile()` / `MigrateDirFiles()` / `OnDictionaryDirChanged()`（均在 cpp 词典工具节）。
- 打包约定（2026-08-30 更新，v2.3.2 修正）：**有功能性修改的新版本编译通过后自动打包** zip 到 `D:\Fast folder\Downloads\压缩文件`，并 git 本地提交；纯文字/UI 文案修改不打包不升版本号。**版本号规则：有功能修改就升 0.1（用户明确"每次 10 进 1"，2.3.1→2.3.2），不得用 `_0` 后缀 zip 代替升版本号**；`_0/_1` 后缀仅用于同版本重复打包防覆盖。zip 内顶层目录为中文固定名「最终幻想14_mod选项汉化工具_AI翻译版」。GitHub 推送必须等用户明确说「推送」才执行；**推送命令必须带代理：`git -c http.proxy=http://127.0.0.1:7890 push origin main`（直连报 Connection was reset；-c 临时参数不改 git 配置）**，仓库 `Lexington-cv2-Lady/FF14-MOD-Options-Chinese-Localization-Tool`（Private）。打包命令：`powershell -NoProfile -Command "& 'D:\Fast folder\Document\C++\*\build_release.ps1' -Zip"`（全 ASCII + 通配符定位中文目录）；git 提交信息走 `write_to_file` 写 UTF-8 到 `D:\Fast folder\Document\_scripts\gitmsg.txt` 再 `git commit -F`；git 在 workspace 根目录直接运行即可（无需 -C 中文路径）。
- 内置文件与个性翻译模板（2026-08-30，v2.2.12 隐藏属性 → v2.3.2 打包修复 → **v2.3.3 移入子文件夹**）：①内置wiki_术语对照.json、内置个性翻译.json 随程序打包，**v2.3.3 起统一放在 exe 旁「内置模板」子文件夹**（zip 格式不保留隐藏属性，隐藏方案失效；项目根源文件仍设 Hidden 防误改）；vcxproj Release|x64 PostBuildEvent：`if not exist "$(OutDir)内置模板\" mkdir` + `xcopy /Y /Q /H` 到 `$(OutDir)内置模板\`；build_release.ps1 复制到 release\内置模板（码点构造 $builtinDirName=0x5185,0x7F6E,0x6A21,0x677F），Step 3 打包前**清理 release 根残留的旧内置文件**（否则 Get-ChildItem -Force 全打包导致 zip 重复、体积变大）。②内置个性翻译.json 预填 FF14 官方种族译法（Hyur=人族、Elezen=精灵族、Lalafell=拉拉菲尔族、Miqo'te=猫魅族、Roegadyn=鲁加族、Au Ra=敖龙族、Hrothgar=硌狮族、Viera=维埃拉族）+「格式参考」说明 key——个性翻译加载逻辑按完整key/英文精确匹配，中文说明 key 永不命中、无副作用；③EnsureCustomDictFile 首次创建词典目录 个性翻译.json 时复制 exe 旁「内置模板」子文件夹的模板，**已存在绝不覆盖（含空 `{}` 文件！用户重装测试时残留的空 个性翻译.json 导致模板没补上，需先删空文件）**；④release/ 目录被 .gitignore 忽略，使用说明.txt 等发布文件不入 git、只随 zip 分发；⑤**zip 打包隐藏文件必须用 .NET `[IO.Compression.ZipFile]::CreateFromDirectory`（需先 `Add-Type -AssemblyName System.IO.Compression.FileSystem`）——PowerShell 的 Compress-Archive 无论传目录还是通配符都会跳过隐藏文件**（v2.2.12 起 zip 一直缺内置文件，v2.3.2 修复），stage 复制也须 `Get-ChildItem -Force | Copy-Item`。
- AI Key 套用行为（2026-08-30，v2.3.2 修正）：套用 AI 配置时，预设/自定义记录**自带 Key 才填入 API_Key 框，无 Key 则清空**（不再保持上一预设的 Key）。自定义记录（如「智谱GLM」）保存时带 Key，套用会自动带出。此前 v2.2.6 曾改为"预设无 Key 保持当前 Key"，用户实测切换预设后 Key 串用（切到大肥鱼仍留智谱 key）要求改回清空逻辑（cpp 5124 行附近）。
- 执行器注意（2026-08-30 实测）：本环境 write_to_file 写 workspace 外 `D:\Fast folder\Document\_scripts\*.ps1` 在 execute_command 视角内容为空、cz.ps1 -File 流程不生效、execute_command 命令行中 `$` 变量会被剥——凡需在脚本中处理中文路径/变量逻辑的，优先把逻辑写进 workspace 内的 build_release.ps1（码点构造中文名）或避免命令行 `$`。
- 部署目录（2026-08-30 更新）：最新版解压即用版部署到 `E:\Program Files\Game-Related\最终幻想14_mod选项汉化工具_AI翻译版`（与 zip 顶层目录同名）；以后更新时保留 `config.user.json`、`日志.json` 等用户数据，只覆盖 exe、config.default.json、使用说明.txt、内置wiki_术语对照.json、内置个性翻译.json，并确保内置文件隐藏属性。
- 个性翻译值格式偏好（2026-08-30）：`个性翻译.json` 与 内置个性翻译.json 的词条值统一为「中文(英文)」格式（英文用半角括号紧贴，如 `"Hyur": "人族(Hyur)"`）；内置模板的「格式参考」说明文案同步描述该格式。注意：写入已存在的隐藏文件时 `[IO.File]::WriteAllText` 可能 Access denied（本机实测），改用 `Copy-Item -Force` 覆盖后再确保 Hidden 属性。

